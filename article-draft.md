# プロジェクトの知識を次の開発に活かす：Project Knowledgeスキル入門

AIエージェントとの開発で、「この認証方式を選んだ理由」を説明したのに、別の会話でも同じ説明をしている。
久しぶりに触る機能について、コードを調べ直すところから始めている。

こうした繰り返しを減らすために、Project Knowledgeは仕様や設計判断、実装・運用の知識を、根拠とともに文書に残します。
コードだけではわからない背景も、次の会話や後日の作業で参照できます。

この記事では、既存プロジェクトからナレッジを作り、質問し、開発中にわかったことを追加する手順を紹介します。

## 問題意識：調べたことや決めた理由が、次の作業に引き継がれない

開発中に得た知識は、会話や担当者の記憶にとどまりがちです。
コードには実装が残っていても、「なぜこの認証方式を選んだのか」「どの運用条件を前提にしたのか」までは読み取れないことがあります。
その結果、会話や担当者が変わるたびに、開発者は背景を説明し直し、AIエージェントは同じ調査を繰り返します。

会話履歴を残すだけでも、後から使うには手間がかかります。
検討途中の案や古い情報が混ざるため、採用した判断とその根拠を探し直す必要があるからです。

Project Knowledgeが解決しようとしているのは、得られた知識を、次の作業で参照できる形に整理し、保ち続ける難しさです。
今後も使う仕様・判断・調査結果を根拠とともに残し、変更に合わせて更新することで、過去の経緯を毎回たどり直す負担を減らします。

## Project Knowledgeで何が楽になるのか

Project Knowledgeは、AIエージェントがプロジェクト固有の知識を整理・更新するためのAgent Skillです。
README、コード、設計資料、開発中のやり取りから、今後も使う情報を選んでMarkdownに保存します。

- 設計判断と理由を残し、変更時に前提を確認できる。
- 仕様や調査結果を再利用し、同じ調査や背景説明を減らせる。
- 開発者もエージェントも文書を読めて、Gitの差分で変更をレビューできる。

保存する内容と更新のタイミングは、プロジェクトに合わせて設定できます。
会話に含まれる一時的な情報や重複は除き、必要な内容を関連文書へ反映します。

## まず作って、質問してみる

### 前提とインストール

Agent Skillsを利用できるAIエージェントと、インストール用のNode.jsを用意します。
通常の利用には、付属スクリプトを実行するためのPython 3.11以上とuvも必要です。

ターミナルで次を実行し、表示されたスキルを選んでインストールします。

```console
npx skills add https://github.com/tetradice/project-knowledge-agent-skills
```

この記事の手順では、作成・更新用の `project-knowledge` と、質問用の `project-knowledge-fast-ask` を使います。
操作案内の `project-knowledge-help` も入れておくと便利です。

以降の `$...` は、ターミナルのコマンドではなく、エージェントへの入力例です。
呼び出し方は、利用するエージェントに合わせてください。

### 既存プロジェクトから作る（init）

対象プロジェクトを開き、次のように依頼します。

```text
$project-knowledge init
```

「プロジェクトナレッジを初期化してください」と自然言語で依頼することもできます。
既存プロジェクトをもとに作られるため、何を残すかを最初から細かく指定する必要はありません。

初期化すると、設定ファイルとナレッジの保存先が作られます。
`AGENTS.md` も作成され、既存のファイルがあれば追記されます。
詳しい配置は後述します。

### 保存した内容に質問する

ナレッジができたら、同じプロジェクトで質問してみます。

```text
$project-knowledge-fast-ask 認証方式と、その採用理由を教えてください。
```

`project-knowledge-fast-ask` は、保存済みのナレッジだけを根拠に回答します。
この例では、認証方式と採用理由が保存されていれば、その内容を確認できます。
コードやWebは追加調査せず、判断材料がなければ情報がないと伝えます。

保存した仕様や判断を確認するときに使い、現在のコードの動作を調べたいときは通常の実装調査として依頼してください。

## 開発でわかったことを追加する（update）

設計方針を決めたときや、実装・運用を変更したときは、ナレッジも更新します。
たとえば、認証方式の採用理由を話し合った後に、次のように依頼します。

```text
$project-knowledge update
```

「今回決めた認証方式と採用理由をナレッジに残してください」と、対象を指定することもできます。
エージェントは既存の知識と収集方針を確認し、今後も使う内容を関連文書へ反映します。
「今回の実装変更をナレッジに反映してください」のように、コードの変更も対象にできます。

### 更新のタイミングを選ぶ

毎回依頼するか、作業に応じて更新を任せるかは、`learning.mode` で選べます。

| 設定 | 更新のタイミング |
| --- | --- |
| `manual` | 明示的に更新を依頼したとき |
| `opportunistic`（既定値） | 作業完了時に候補を評価し、保存価値があれば更新 |
| `aggressive` | 作業完了時に、より広く候補を評価して更新を判断 |

手動で試したい場合は、次のように依頼します。

```text
今後は明示的に依頼したときだけ更新してください。
```

設定を見直すときは `$project-knowledge config` を使います。
自動更新でも、評価するのは調査や実装などの作業が完了した時点です。
発言や操作を毎回記録するわけではありません。

### 保存先の書き込み設定

更新には、保存先が書き込み可能である必要があります。
ルート設定の `access` は、省略すると `read-write` になり、通常の新規初期化後は更新できます。
読み取り専用の場合は、設定を変更する意図を明示してから更新します。
`learning.mode` で自動更新を選んでも、読み取り専用の保存先には書き込みません。

## 知識はどこに保存されるのか

既定の配置で作られる主なファイルは次のとおりです。

```text
プロジェクト/
├── project-knowledge.yaml
├── AGENTS.md
└── project-knowledge/
    ├── manifest.yml
    ├── knowledge-policy.md
    ├── state.yml
    └── docs/
        ├── index.md
        ├── log.md
        ├── … 知識をまとめたMarkdown文書
        └── references/
            ├── interactions/
            └── user-statements/
```

| ファイル・フォルダ | 役割 |
| --- | --- |
| `project-knowledge.yaml` | 保存先、呼称、アクセス設定を登録する |
| `knowledge-policy.md` | 何を残し、どう更新するかを定める |
| `docs/` | 知識の本文を保存する。`index.md` が文書を探す入口 |
| `docs/references/` | 参考資料ややり取りの記録を保存する |
| `manifest.yml` | ナレッジの形式情報を保持する |
| `state.yml` | 差分検出などに使う、再構築可能な作業状態を保持する |

日々の利用では、本文と収集・更新方針を確認できれば十分です。
複数の保存先を「レイヤー」として登録することもできますが、最初は既定の一つで始められます。

## 用途に合わせて使う関連スキル

作成・更新用の `project-knowledge` に加え、目的別のスキルを使い分けられます。

| やりたいこと | 使うスキル |
| --- | --- |
| 操作方法を確認する | `project-knowledge-help` |
| 保存済みの内容・構成・更新方針を把握する | `project-knowledge-inspect` |
| 保存済みの知識に質問する | `project-knowledge-fast-ask` |
| 人が読むMarkdownやオフラインHTMLにまとめる | `project-knowledge-publish` |
| 文書の重複や肥大化を調べ、構造を整理する | `project-knowledge-audit` |
| 同じ作業をナレッジなし／ありで実行して比較する | `project-knowledge-benchmark` |

操作に迷ったときは、次のように対象を指定して確認できます。

```text
$project-knowledge-help update
```

内容の正確さや文書構造を見直すときは、検査と修正を分けて依頼します。

| 対象 | 検査のみ | 修正・改善まで |
| --- | --- | --- |
| 現在の実装と内容が一致するか | `project-knowledge verify` | `project-knowledge fix` |
| 文書の重複や肥大化などの構造 | `project-knowledge-audit audit` | `project-knowledge-audit refactor` |

## まずは一つの仕様を質問する

手元のプロジェクトで初期化し、よく確認する仕様を一つ質問してみてください。
答えに足りない背景があれば、その判断理由を追加します。
次の作業で参照し、説明や調査の手間が減るか試してみてください。

導入手順と各スキルの詳細は、[Project Knowledgeのリポジトリ](https://github.com/tetradice/project-knowledge-agent-skills)で確認できます。

---

編集メモ（公開時に削除）：2026年9月14日のローカル実装と仕様を確認して作成した草稿。`access` の省略値は `project_config.py` と `references/project-config.md` の `read-write` に合わせた。READMEと `references/init.md` には読み取り専用とする記述が残っているため、公開前に説明の整合を確認する。
