# プロジェクトの知識を、次の開発にも活かす — Project Knowledgeスキル入門

AIエージェントとの開発で、「この認証方式を選んだ理由」を説明したのに、別の会話でも同じ説明をしている。
久しぶりに触る機能について、コードを調べ直すところから始めている。
そんな場面で使えるのが、Project Knowledgeスキルです。

プロジェクトの仕様や設計判断、実装・運用の知識をファイルに残し、後の質問や作業で参照できるようにします。
この記事では、既存プロジェクトからナレッジを作り、質問し、開発中にわかったことを追加するまでを紹介します。

## Project Knowledgeで何が楽になるのか

Project Knowledgeは、AIエージェントにプロジェクト固有の知識を整理・保守させるためのAgent Skillです。
README、コード、設計資料、開発中のやり取りから、今後も使う情報を選んで保存します。

- 設計判断とその理由を残せるため、変更時に前提を確認し直せる。
- 調査結果や仕様を残せるため、同じ調査や背景説明を繰り返さずに済む。
- 根拠とともにMarkdownで管理するため、エージェントも開発者も必要な情報を確認できる。
- Gitで差分を管理するため、知識の変更もレビューできる。

保存する内容と更新のタイミングは、プロジェクトに合わせて設定できます。
会話をそのまま蓄積するのではなく、将来使う情報だけを関連文書へ反映します。

## まず作って、質問してみる

### 前提とインストール

Agent Skillsを利用できるAIエージェントと、インストール用のNode.jsを用意します。
通常の利用には、付属スクリプトを実行するためのPython 3.11以上とuvも必要です。

ターミナルで次を実行し、表示されたスキルを選んでインストールします。

```console
npx skills add https://github.com/tetradice/project-knowledge-agent-skills
```

この記事の手順では、作成・更新用の `project-knowledge` と、質問用の `project-knowledge-fast-ask` を使います。
操作案内の `project-knowledge-help` も入れておくと便利です。

以下の `$project-knowledge ...` は、エージェントへの入力例です。
ターミナルのコマンドではありません。
スキルの呼び出し方は、利用するエージェントに合わせてください。

### 既存プロジェクトから作る（init）

対象プロジェクトを開き、次のように依頼します。

```text
$project-knowledge init
```

または、自然言語で次のように依頼できます。

```text
プロジェクトナレッジを初期化してください。
```

初期化を指示すると、既存プロジェクトをもとにナレッジが作られます。
何を残すかを最初から細かく指定する必要はありません。

初期化直後には、次のフォルダとファイルが作られます。

```text
プロジェクト/
├── project-knowledge.yaml
├── AGENTS.md
└── project-knowledge/
    ├── .gitignore
    ├── knowledge-policy.md
    ├── manifest.yml
    ├── state.yml
    ├── docs/
    │   ├── index.md
    │   ├── log.md
    │   └── references/
    │       ├── index.md
    │       ├── interactions/
    │       │   └── index.md
    │       └── user-statements/
    │           └── index.md
    └── published/
        ├── html/
        └── markdown/
```

通常の初期化では、調査結果に応じたナレッジ本文も`docs/`の下に追加されます。

### 保存した内容に質問する

ナレッジができたら、同じプロジェクトで質問してみます。

```text
$project-knowledge-fast-ask 認証方式と、その採用理由を教えてください。
```

または、自然言語で次のように依頼できます。

```text
認証方式と、その採用理由を教えてください。
```

`fast-ask` は、保存済みのナレッジから必要な情報を探して回答します。
先ほどの例が保存されていれば、採用した方式と理由を確認できます。

このスキルが回答の根拠にするのは、Project Knowledge内の情報だけです。
コードやWebを追加調査せず、判断材料がなければ情報がないと伝えます。
そのため、保存済みの判断を確認する用途に向いています。
現在のコードでどう動くかを調べたい場合は、通常の実装調査として依頼します。

## 日々の開発で知識を育てる（update）

初期化の後は、設計判断を決めたときや、実装・運用上の変更があったときに情報を追加します。
たとえば、認証方式の採用理由を話し合った後なら、次のように依頼できます。

```text
$project-knowledge update
```

または、自然言語で次のように依頼できます。

```text
今回決めた認証方式と採用理由をナレッジに残してください。
```

エージェントは既存の知識と収集方針を確認し、保存する価値のある内容を関連文書へ反映します。
「今回の実装変更をナレッジに反映してください」のように、コードの変更を対象にすることもできます。

更新には、保存先が書き込み可能である必要があります。
現在のルート設定では `access` を省略すると `read-write` となり、通常の新規初期化後は更新できます。
読み取り専用で運用している場合は、設定を変更する意図を明示してから更新します。

毎回依頼するか、作業に応じて更新を任せるかは、`learning.mode` で選べます。

| 設定 | 更新のタイミング |
| --- | --- |
| `manual` | 明示的に更新を依頼したとき |
| `opportunistic` | 作業が完了した時点で候補を評価し、保存価値があるとき。既定値 |
| `aggressive` | 作業完了時に、より広く候補を評価するとき |

まず手動で試したいなら、次のように設定します。

```text
$project-knowledge config
```

または、自然言語で次のように依頼できます。

```text
今後は明示的に依頼したときだけ更新してください。
```

自動更新を選んだ場合も、毎回の発言や操作を記録するわけではありません。
調査や実装などの作業単位が終わった時点で評価し、一時情報や重複は除外します。
書き込み可否と更新タイミングは別の設定なので、読み取り専用の保存先は自動更新されません。

## 知識はどこに保存されるのか

既定の配置では、プロジェクト内に次のようなファイルができます。
ここでは主なものだけを示します。

```text
プロジェクト/
├── project-knowledge.yaml
└── project-knowledge/
    ├── manifest.yml
    ├── knowledge-policy.md
    ├── state.yml
    └── docs/
        ├── index.md
        ├── log.md
        ├── … 知識をまとめたMarkdown文書
        └── references/
```

入口は `project-knowledge.yaml` です。
ナレッジの場所や呼称、アクセス設定を登録します。
複数の保存先をレイヤーとして登録することもできますが、最初は既定の一つで始められます。

知識の本文は `docs/` に入り、`index.md` が文書を探す入口になります。
`references/` には参考資料ややり取りの記録などを置きます。
何を残すか、どのように更新するかは `knowledge-policy.md` で扱います。

`manifest.yml` はナレッジの形式情報、`state.yml` は差分検出などに使う再構築可能な作業状態です。
日々の利用では、本文と方針を確認できれば十分です。

一方、インストールするスキル側は、入口となる `SKILL.md`、手順や仕様の `references/`、処理を補助する `scripts/`、ひな形の `templates/` で構成されています。
操作を依頼すると、エージェントが必要な手順を読み、付属スクリプトも使って作業します。

## 用途に合わせて使う関連スキル

作成・更新を担うメインスキルに加え、目的別の専用スキルがあります。
最初からすべて覚える必要はありません。

| やりたいこと | 使うスキル |
| --- | --- |
| 操作方法を確認する | `project-knowledge-help` |
| 保存済みの内容・構成・更新方針を把握する | `project-knowledge-inspect` |
| 保存済みの知識に質問する | `project-knowledge-fast-ask` |
| 人が読むMarkdownやオフラインHTMLにまとめる | `project-knowledge-publish` |
| 文書の重複や肥大化を調べ、構造を整理する | `project-knowledge-audit` |
| 同じ作業をナレッジなし／ありで実行して比較する | `project-knowledge-benchmark` |

操作に迷ったときは、次のように対象を指定して確認できます。

```text
$project-knowledge-help update
```

または、自然言語で次のように依頼できます。

```text
Project Knowledgeを更新する方法を教えてください。
```

ナレッジの内容が現在の実装と一致するか調べるなら、メインスキルの `verify`、問題の修正まで依頼するなら `fix` を使います。
文書構造については、専用スキルの `audit` が検査、`refactor` が改善を担当します。
いずれも検査と修正は別の操作です。

## 最初に残す、一つの判断

手元のプロジェクトで初期化し、よく確認する仕様を一つ質問してみてください。
答えに足りない背景があれば、その判断理由を追加します。
次の作業で同じ内容を参照できれば、自分のプロジェクトでどう使うかが見えてきます。

導入手順と各スキルの詳細は、[Project Knowledgeのリポジトリ](https://github.com/tetradice/project-knowledge-agent-skills)で確認できます。

---

編集メモ（公開時に削除）：2026年9月14日のローカル実装と仕様を確認して作成した草稿。`access` の省略値は `project_config.py` と `references/project-config.md` の `read-write` に合わせた。READMEと `references/init.md` には読み取り専用とする記述が残っているため、公開前に説明の整合を確認する。
