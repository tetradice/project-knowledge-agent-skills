# 技術記事の構成案

タイトル案：**プロジェクトの知識を、次の開発にも活かす — Project Knowledgeスキル入門**

想定読者は、AIエージェントにプロジェクトの事情を繰り返し説明したり、過去の設計判断を探し直したりしている開発者。
本文は3,000〜4,000字程度を目安とし、使い方に半分ほどを割く。
「認証方式を調べる → 採用理由を記録する → 後の作業で質問する」という一つの例を通して説明する。

## 1. Project Knowledgeで、何が楽になるのか

- **解決したい課題**：会話に埋もれた設計判断、繰り返すプロジェクト調査、引き継ぎ時の説明負担。
- **スキルの役割**：仕様・設計判断・実装・運用の知識を、AIエージェントが継続的に整理する。
- **特長と利点**：根拠とともにMarkdownへ残し、後の質問や作業で参照できる。収集・更新方針もプロジェクトに合わせられる。
- 冒頭で「認証方式の採用理由を記録し、次の作業で参照する」という利用例を短く示す。効果の数値は実測がある場合だけ載せる。

## 2. まず作って、質問してみる

### 2.1 前提とインストール

- Agent Skillsを利用できるAIエージェント、導入用のNode.js／npx、付属スクリプト実行用のuvを案内する。
- READMEの `npx skills add https://github.com/tetradice/project-knowledge-agent-skills` を掲載する。
- 最初の体験で使う `project-knowledge` と `project-knowledge-fast-ask` を案内する。
- 端末で実行するインストールコマンドと、エージェントに入力する依頼例を区別する。

### 2.2 既存プロジェクトから作る（init）

- `$project-knowledge init` の依頼例と、README・コード・設定・設計資料などから初期ナレッジを作る流れ。
- 生成されたナレッジ文書の短い例を示し、「何が手に入るか」を見せる。
- 空で始めたい場合は、その旨を明示できることを補足する。

### 2.3 保存した内容について質問する

- 依頼例：`$project-knowledge-fast-ask 認証方式と、その採用理由を教えてください。`
- 回答と根拠の参照例を示す。記録がなければ情報不足として扱うことも伝える。
- `fast-ask` は保存済みナレッジだけを根拠に回答するため、現在の実装の調査とは用途が異なることを短く説明する。

## 3. 日々の開発で知識を育てる（update）

- **更新の準備**：init直後は読み取り専用。更新を有効にする明示依頼と、`project-knowledge.yaml` の `access: read-write` を案内する。
- **記録する場面**：設計判断を決めた後、実装を変更した後、運用上の注意点がわかった後。
- **依頼例**：`$project-knowledge update 今回決めた認証方式と採用理由をナレッジに残してください。`
- **運用の選択**：明示依頼時に更新する `manual` を入口に、作業完了時に候補を評価する `opportunistic`／`aggressive` を短く紹介する。
- **残す内容の選別**：将来役立つ情報を根拠とともに残し、一時情報や重複を増やさない。

## 4. 知識はどこに、どのように保存されるのか

- **利用先プロジェクトの構成**：小さなファイルツリーで、`project-knowledge.yaml`（場所・アクセス設定）、`docs/`（知識本文・索引・参考資料）、`knowledge-policy.md`（収集・運用方針）を説明する。`manifest.yml`（形式情報）と `state.yml`（再構築可能な作業状態）は一言ずつ添える。
- **スキル側の構成**：`SKILL.md`（操作の入口）、`references/`（手順・仕様）、`scripts/`（処理の補助）、`templates/`（ひな形）を簡潔に紹介する。
- メタデータやスキーマの全項目は列挙せず、詳細資料へ誘導する。

## 5. 用途に合わせて使う関連スキル

「サブスキル」は、用途ごとに呼び出す専用スキルとして紹介する。
各スキルの詳しい操作説明は省き、次の一覧で選べるようにする。

| やりたいこと | スキル |
| --- | --- |
| 操作方法を確認する | `project-knowledge-help` |
| 保存されている内容・構成・更新方針を知る | `project-knowledge-inspect` |
| 保存済みの知識に質問する | `project-knowledge-fast-ask` |
| 人が読むMarkdown・オフラインHTMLにまとめる | `project-knowledge-publish` |
| 重複や肥大化などを調べ、必要に応じて構造を整理する | `project-knowledge-audit`（audit／refactor） |
| 同じ作業をKnowledgeなし／ありで比較する | `project-knowledge-benchmark` |

内容の検証・修正はメインスキルの `verify`／`fix`、設定変更は `config` として補足する。
検査と修正は別操作で、専用スキルが自動的に連続実行されるわけではないことを簡潔に伝える。

## 6. 最初に試す小さな使い方

- 一つのプロジェクトで初期化し、一つ質問し、今回の設計判断を一つ残す、という着手手順で締める。
- インストール先とヘルプへの導線を置く。

---

構成の確認元：リポジトリの `README.md`、各 `skills/*/SKILL.md`、`project-knowledge` の初期化・更新・設定・データ形式の参照資料。
これは記事本文ではなく、各節で扱う内容を示した構成案。
