# 複数レイヤーの初期化・分割移行の手順案

2026-09-15。検討案であり、以下の新しい操作・補助機能は未実装。
既存のSkill、ルート設定、データ形式、stateの定義済みバージョンは変更しない。

## 方針

初期化と分割移行の両方で、最初にレイヤー定義を確定し、その定義から配置を決める。
途中でAIがレイヤー数、ID、パス、振り分け基準を作り直さない。
意味の判断はAIが行い、設定検査、配置、参照の書換え、完了判定は機械的に行う。

| 操作 | 推奨する入口 | 対象 |
| --- | --- | --- |
| 複数レイヤーで新規導入 | `$project-knowledge init` にレイヤー構成を指定 | 未登録のプロジェクト |
| 単一レイヤーを分割 | 新設する `$project-knowledge split` | 指定した既存1レイヤーと、そこから作る新規レイヤー |
| 分割後の設定変更 | `$project-knowledge config` | ルート設定、または明示した1レイヤーの運用設定 |

`split`は初期化・配置変更をまとめた操作とする。
通常の`update`や保守的な`refactor`に、複数レイヤーを変更する責務を混ぜない。
全体再編、既存の複数レイヤー間の再分配、文書の意味を変える編集は、この初版の分割手順には含めない。

## 1. 両操作で先に確定する事項

### レイヤー定義

利用者の明示値を優先し、不足する機械的な値には下表の既定値を使う。
意味のある境界が不明な場合だけ、用途と保存対象について質問する。
すでに十分な指定がある場合は、設定の再承認を必須にしない。

| 項目 | 決め方 |
| --- | --- |
| レイヤー数・用途 | 利用者の指定を採用。指定がなければ現状の単一レイヤー初期化を維持。複数指定があるのに境界が不明なら質問する |
| `id` | 指定値を採用。未指定なら用途から有効なIDを一度生成し、以後固定。既存IDは変更しない |
| `name` | 用途を表す名称。既存の名称は指定がなければ維持 |
| `path` | 既存は維持。新規`default`は`./project-knowledge`、その他は`./project-knowledge-<id>` |
| `description` | 「保存する情報」と「他レイヤーへ送る情報」を具体化。複数構成では`default`にも記載 |
| `access` | 新規は明示的に`read-write`を記載。指定があればその値。既存の権限は維持 |
| `optional` | 新規は`false`。今回作成するレイヤーの未完成を隠す目的では使わない |
| `auto_select` | 新規は`true`。機密用・手動指定専用など明示用途があれば`false` |
| `write_target` | 明示指定を採用。既存は維持。新規複数構成で指定がなければ明示的に`null` |

全レイヤーのID・名前は一意とする。
新規パスは互いに同一・入れ子にせず、既存レイヤーとも重ねない。
入力順を設定の順序とし、ファイル処理順は相対パス順に固定する。
これらの新規作成時の制限を、既存設定全般の仕様変更として遡及適用しない。

`write_target`は曖昧な新情報の通常運用に使う設定であり、初期収集や移行時の分類には使わない。
移行中の書込み先は、次の配置表の明示IDで決める。

### 振り分け基準と配置表

1. 利用者が指定した文書・ディレクトリとレイヤーの対応を最優先にする。
2. 明示対応がなければ、文書の主題とレイヤーの保存対象・除外対象を照合する。
3. 適合先が1件なら採用する。0件または複数件なら未決定として残す。
4. 初期化では未決定の保存先を質問する。分割では元レイヤーを残せる場合に限り、未決定文書を元に残す。
5. 同じ主張を持つConceptを複数レイヤーへ複製しない。複数主題が混在する既存文書も、この操作では本文分割せず元に残す。

配置表には、元ファイルまたは収集候補、移動先ID、移動先相対パス、判定理由、依存Referenceを記載する。
元レイヤーを廃止する指定があり、未決定文書がある場合は適用を止める。
レイヤー間の境界が重複するときは、境界を修正してから表を確定する。
表の確定後は、再実行でも同じ表を使い、分類を再推論しない。

## 2. 複数レイヤーでの初期化

### 入力例

```text
$project-knowledge init
「製品仕様」と「開発・運用」の2レイヤーで初期化してください。
製品仕様には利用者向け機能・業務ルール、開発・運用には実装判断・運用手順を保存します。
```

### 実行順序

1. **未登録を確認する。** ルート設定が存在する場合は新規全体初期化に進まない。追加導入か分割かを依頼内容から判定する。不正な設定を「未登録」と扱わない。
2. **レイヤー定義とPolicyを用意する。** 標準Policyを基に、各レイヤーの用途を反映する。定義済みの権限・更新モードを黙って変更しない。
3. **全レイヤーの骨組みを準備する。** manifest、Policy、索引、ログ、local stateなどを最終配置先に作る。この時点ではルート設定を登録しない。既存の無関係なディレクトリへ上書きしない。
4. **プロジェクトを一度調査する。** README、代表的な実装、設定、設計資料から保存候補を収集し、配置表を作る。レイヤーごとに独立した調査を繰り返さない。
5. **レイヤー単位で本文を生成する。** 配置表に従ってConceptとReferenceを生成し、各索引から到達可能にする。保存価値のある根拠があるレイヤーでは通常Conceptを生成する。根拠がないレイヤーは空である理由を報告し、件数合わせで内容を作らない。
6. **登録前に検査する。** 形式、配置表との対応、参照解決、索引、設定候補を検査する。対象がすべて揃うまで登録しない。
7. **登録する。** 生成元コメントを含む`project-knowledge.yaml`を最後に作成する。AGENTS.mdと`.gitignore`の管理ブロックも全レイヤーに対応させ、重複させない。
8. **登録後の解決を確認する。** 共通設定CLIが全対象を解決できること、通常の読取りと指定レイヤーへの書込み選択が正しいことを確認する。

「空で初期化」の明示時は4・5を省略する。他の設定・形式・登録の検査は行う。
初期化時の検査は作成物の整合性確認であり、網羅的な`verify`を別操作として自動実行しない。

### 読み取り専用で開始する場合

新規未登録のレイヤーは、依頼された初期内容の準備後に`access: read-only`で登録する。
登録後に一時的に権限を上げて生成する手順にはしない。
既存の読み取り専用レイヤーを初期化し直す場合は、現行の書込み禁止を守る。

### 再開と完了条件

中断時には準備済みファイルと配置表を残し、同じ定義で不足分だけを再開する。
前回生成後に変更されたファイルは、機械的に上書きせず差分を確認する。
全対象が有効、未決定の配置がゼロ、各文書が索引から到達可能、参照が解決可能なら完了とする。
空の理由が説明されたレイヤー、または明示的な空初期化は有効な完了状態とする。

## 3. 単一レイヤーから複数レイヤーへの分割移行

### 入力例

```text
$project-knowledge split
現在のナレッジから開発・運用情報を別レイヤーへ分けてください。
製品仕様と振り分けが決まらない文書は既存レイヤーに残してください。
```

既定動作は、既存レイヤーのID・パス・権限を維持し、確定した文書だけを新規レイヤーへ取り出す。
元レイヤーの廃止は明示依頼時だけ扱い、文書と履歴の移管先がすべて確定することを条件とする。

### 実行順序

1. **元レイヤーを解決する。** 元IDを固定し、形式、書込み権限、既存の不整合を記録する。元が読み取り専用なら、権限変更の指示なしに進まない。
2. **移行前の状態を記録する。** 設定、対象文書、索引、Policy、ログとファイル内容の指紋を保存する。Git未追跡の対象も復旧できるようにする。
3. **定義・配置表を確定する。** 元に残す文書も含め、全Conceptに一つの移行結果を割り当てる。Referenceと本文リンクの依存関係を追い、変更が必要な参照元も列挙する。
4. **新レイヤーの候補を生成する。** 元ファイルを保持したまま、移動先へ文書をコピーする。新レイヤーのPolicyは元の収集方針・運用設定を引き継ぎ、指定された用途だけを調整する。元Policyを黙って緩めない。
5. **参照と索引を更新した候補を作る。** 新レイヤーと元レイヤーの変更後候補を揃え、下記の移管規則に従って参照を再計算する。
6. **候補全体を検査する。** 文書の対応、claimと根拠の保持、形式、リンク、各索引からの到達性を確認する。移行前からの問題と今回増えた問題を区別し、新しい問題があれば切り替えない。
7. **適用直前に再確認する。** 記録した入力から変更がないことを確認する。変わっていれば候補を作り直す。以後の切替区間では、同一プロジェクトへの他のKnowledge操作を止める。
8. **切り替える。** 元レイヤーの索引・文書・参照を更新し、移動済み文書を元から除く。新レイヤーをルート設定へ登録し、必要な管理ブロックを更新する。設定の公開を最後にする。
9. **最終状態を確認する。** 設定解決、参照、索引、明示的な書込み先選択を再確認する。失敗時はこの操作による変更だけを移行前へ戻す。
10. **移行結果を残す。** 移動元・移動先、残した文書と理由、Referenceの複製、検査結果を報告する。復旧用コピーは成功確認後に今回作成したものだけを片付ける。

複数ファイルの切替は、設定ファイルの置換だけでは不可分にならない。
切替中の操作停止と復旧記録が必要であり、その仕組みを実装するまでは「安全に自動移行できる」と扱わない。

### 文書・Reference・管理情報の移管規則

| 対象 | 固定する扱い |
| --- | --- |
| Concept | 元文書のclaimとmetadataを維持して移動。配置変更だけを根拠に`verified`を更新しない。変更履歴は記録する |
| 専用Reference | 移動するConcept群だけが使い、他の参照元がなければ一緒に移す |
| 共有Reference | 元を残し、必要な移動先へ内容とprovenanceを維持したコピーを作る。移行記録でコピー元を示す。移管先Policyが保存を許さなければ停止する |
| User Statement / Interaction | Referenceと同様に扱い、Conceptからの到達性を維持する |
| 本文リンク | リンクを持つMarkdownファイル基準で相対パスを再計算する |
| `sources[].resource` | 移管先のbundle root（`docs/`）基準で再計算する。project-artifactの実体と外部URIは変えない |
| レイヤーをまたぐConceptリンク | 両レイヤーが必須で、現行resolver・validatorが扱える参照に限る。未対応なら黙って独自URIを導入せず、その文書群を元に残すか計画を見直す |
| `index.md` | 各レイヤーで作り直し、管理対象の全ページへ到達できるようにする |
| `log.md` | 元の履歴を元に残し、両側へ移行結果を追記する。元廃止時は履歴の保存先を配置表で決める |
| manifest | 既存の形式・版を維持して新規生成。既存ファイルを他レイヤーの固有情報ごとコピーしない |
| state・キャッシュ | 新レイヤーへコピーしない。新規stateを生成し、元の変更対象に関する状態も再構築する。既存のschemaを使う |
| publish成果物 | 再生成しない。必要なら利用者へ別操作として案内する |

同名の移管先ファイルが衝突した場合は、自動連番による解消をしない。
同一内容・同一provenanceなら共有できるかを判定し、それ以外は配置表で別パスを確定してから再開する。
移動元レイヤーの外にある参照元も調べ、読み取り専用の参照元を変更しなければ成立しない計画は適用しない。

### 完了条件

- 元Conceptのすべてが「元に残す」または「一つの移管先へ移す」に対応し、主張と根拠が欠落していない。
- 移行対象について新しい参照切れ、索引からの到達不能、配置の重複がない。
- 既存不整合は記録され、今回の操作で増えていない。移行可否を判定できない不整合がある場合は停止している。
- 設定に登録した全レイヤーが解決でき、権限・Policy・書込み先が確定した定義と一致する。
- 再実行時に完了済みの移動を繰り返さず、設定・ログ・管理ブロックを重複生成しない。

## 4. 分割後の通常運用

参照時には全レイヤーを対象にし、文書の出典レイヤーを示す。
更新時には現行の選択順序を維持し、利用者の明示指定、明確な名称・ID、用途とPolicyに適合する一意な候補、`write_target`の順で一つを選ぶ。
どれでも決まらない場合は保存先を質問する。
複数領域にまたがる依頼は、レイヤーごとに明示対象を持つ複数の更新へ分ける。

共有Referenceのコピーは自動同期しない。
将来その根拠が更新されたときは、移行記録とsourceから影響先を確認し、書込み可能な対象ごとに更新する。
複製が頻発する場合は、次の構成見直しでレイヤー境界を検討する。
通常の更新のたびに自動分割・自動移動を行わない。

## 5. 現行実装との差分と実装順序

現在の[初期化スクリプト](skills/project-knowledge/scripts/init_project.py)は、未登録プロジェクトでは`default`だけを作る。
登録済みでは`--layer`で一件を指定できるが、`--prepare`は未登録専用である。
設定を先に手書きして順次初期化する方法では、複数レイヤーの準備完了を待って公開する流れを保証できない。

1. [ルート設定仕様](skills/project-knowledge/references/project-config.md)を正本とし、[init手順](skills/project-knowledge/references/init.md)などに残る「省略時は読み取り専用」の説明を整合させる。現行仕様の省略時`read-write`に合わせる。
2. `init`に確定済みレイヤー定義を渡せるようにする。設定候補を検査し、全対象の準備と最後の登録を分離する。現行の引数と単一初期化は維持する。
3. `split`の入口と専用Referenceを追加する。通常操作は一件への書込みを維持し、分割時だけ元と移管先の固定集合を扱う。対象外レイヤーへの書込みは許さない。
4. 配置表に基づく参照書換え、候補検査、切替、復旧、再開を補助スクリプトに集約する。操作記録は一時ファイルとし、既存ルート設定やstateのschemaへ移行専用項目を追加しない。
5. helpとconfigへ導線を追加し、ルート設定とレイヤー内Policyの変更を区別する。

候補検査は、物理的な準備場所と最終配置先を区別できる必要がある。
一時ディレクトリ基準でリンクを書き換えず、最終配置先での参照解決を検査する。
新規未登録の候補だけを検査できる内部入口を設け、通常操作の登録必須ルールは維持する。

実装時には、2レイヤー初期化、空初期化、read-only登録、中断再開、元を残す分割、共有Reference、相対source、衝突、読み取り専用参照元、切替失敗からの復旧を検証する。
文面の一致ではなく、同じ配置表から同じ配置になること、情報が失われないこと、再実行で重複しないことを確認する。
