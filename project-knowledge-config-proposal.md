# Project Knowledgeのルート設定ファイル案

2026-09-13時点の検討案。
この文書の初期段階（0〜1レイヤー）は実装済みであり、複数レイヤーの同時利用は将来仕様として区別する。
運用手順の正本は[ルート設定仕様](skills/project-knowledge/references/project-config.md)を参照する。
既存のSkill、データ形式、スキーマのバージョンは変更しない。

推奨する構成は、プロジェクト直下の`project-knowledge.yaml`を必須の入口とし、その`layers`で利用するナレッジの保存先を指定する形である。
設定ファイルが存在しなければ、`project-knowledge/`や`manifest.yml`があってもProject Knowledgeの対象として扱わない。
設定ファイルの存在による利用意思と、参照先が正常に利用できることは別々に判定する。

## ファイル形式と最小構成

ファイル名は`project-knowledge.yaml`に統一する。
YAMLを選ぶ理由は、コメントを記述でき、既存のmanifestやPolicyの設定と記法を揃えられるためである。
`.yml`、JSON、TOMLの別名は初期仕様では受け付けず、複数形式の探索順や競合規則を持ち込まない。

1レイヤーを利用する最小構成は次のとおりである。

```yaml
version: "1.0"
layers:
  - id: default
    path: ./project-knowledge
```

`version`、`layers`と、各要素の`id`、`path`は必須とし、省略値を補わない。
`{}`、空ファイル、コメントだけのファイルは、必須の`layers`がないため設定エラーとする。
`null`、配列、文字列などをトップレベルに記述した場合も設定エラーとする。
この最小構成は`access`の省略により読み取り専用となる。

## 初期化時の生成サンプル

`project-knowledge` Skillによる初期化では、次の設定を生成する。
生成ファイルの先頭行には、生成元を示す次のコメントを必ず記載する。

```yaml
# この設定ファイルは project-knowledge skill で生成されました。
version: "1.0"
layers:
  - id: default
    path: ./project-knowledge
```

初期化では`id: default`を使い、`description`、`access`、`optional`、`write_target`を省略する。
この設定は省略規則により、説明なし、`access: read-only`、`optional: false`、`write_target: null`として扱う。
初期化後に知識を記録する場合は、`access: read-write`を明示する。
生成元コメントは生成時の要件であり、手書きの設定ファイルの有効性判定には使わない。

`path`は`docs/`ではなく、`manifest.yml`、`knowledge-policy.md`、`docs/`を含むナレッジ管理ディレクトリを指す。
ナレッジの内部構造は既存のFormat 1.0に従う。

## 設定項目

| キー | 型 | 省略時 | 意味 |
| --- | --- | --- | --- |
| `version` | 文字列`"1.0"` | 省略不可 | ルート設定ファイルの仕様バージョン |
| `layers` | オブジェクトの配列 | 省略不可 | 利用対象とするナレッジの一覧 |
| `layers[].id` | 空でない文字列 | 省略不可 | パス変更後も維持できる識別子 |
| `layers[].path` | 空でない文字列 | 省略不可 | 設定ファイルからの相対パス |
| `layers[].description` | 文字列 | `id: default`の場合だけ省略可。それ以外は必須 | LLMがレイヤーの用途や対象範囲を判断するための自然言語の説明 |
| `layers[].access` | `read-only`または`read-write` | `read-only` | そのレイヤーへの書き込み可否 |
| `layers[].optional` | boolean | `false` | ディレクトリが存在しない場合に、そのレイヤーを省略できるか |
| `write_target` | レイヤーIDまたは`null` | 下記の規則 | 更新先を明示しない操作で使用する既定の書き込み先 |

`version`の初期値は文字列`"1.0"`とし、YAMLでは引用符を付けて記述する。
未対応のバージョン、数値型、欠落は設定エラーとし、自動変換や書き換えを行わない。
ユーザーが指示しない限り、設定ファイルのバージョンを自動で上げない。
この値はSkillの版、Knowledgeの`format_version`、stateの内部スキーマとは独立して扱う。

暗黙のレイヤーやID、パスを追加しない。
`layers: [{}]`や、要素の`id`または`path`が欠けた設定はエラーとする。
`layers: []`は有効な設定とし、利用対象がゼロである状態を表す。

レイヤーが1件の場合も、各レイヤーの`id`と`path`を必須にする。
`id`は`[a-z][a-z0-9_-]*`に制限し、設定内で一意にする。
表示名が必要になるまでは`id`を表示にも使う。

`description`は`id`が`default`以外の場合に必須とし、複数行の自然言語も記述できる。
`default`以外では、省略、空文字列、空白だけの文字列を設定エラーとする。
`id: default`の場合は省略可能で、省略、空文字列、空白だけの文字列を「説明なし」として扱う。
どのIDでも、`null`や文字列以外は型エラーとする。
LLMはこの説明を、質問に関連するレイヤーの選択、内容の適用範囲の理解、記録先の候補提示に使う。
説明がない`default`レイヤーも利用対象に含め、説明だけを根拠に関連するナレッジが存在しないと断定しない。
説明は`access`、`write_target`、各レイヤーのPolicyを上書きせず、レイヤー間の正しさの優先順位も定めない。

`write_target`を省略した場合、登録されたレイヤーが1件で、かつ`read-write`の場合に限り、そのIDを採用する。
それ以外は`null`とし、読み取りは可能だが、書き込み操作には対象IDの指定を必要とする。
明示的な`null`も同じ意味であり、対象IDを指定した書き込みまで禁止する設定ではない。
複数レイヤーで書き込み可能なものが1件だけでも、暗黙には選ばない。

`write_target`で未知のIDや`read-only`のIDを指定した場合は設定エラーとする。
`optional: true`の書き込み先が欠落していても、他のレイヤーへ書き込み先を切り替えない。
その場合は読み取りだけを継続し、対象への書き込み要求をエラーにする。

## 設定の探索とパス解決

プロジェクトルートを明示できる操作は、その直下だけで`project-knowledge.yaml`を確認する。
明示したルートに設定がなければ、親プロジェクトを探して代用しない。

作業ディレクトリから自動探索する場合は、次の規則を用いる。

1. 現在のディレクトリから親方向に探索し、最初の`project-knowledge.yaml`を採用する。
2. Gitの作業ツリー内では、そのルートまでを探索範囲とする。ネストしたリポジトリやworktreeの境界を越えない。
3. Git管理外では、呼び出し元が指定したワークスペースルートまでとする。境界を特定できなければ現在のディレクトリだけを確認する。
4. 子ディレクトリを再帰探索しない。複数の設定を親子で自動マージしない。
5. 発見した設定が壊れていれば、その場所でエラーにする。親の設定や既定パスにフォールバックしない。

採用した設定ファイルの所在ディレクトリを、この実行におけるプロジェクトルートとする。
モノレポでは、子プロジェクトに独自設定があればそれを採用し、なければ探索境界内の親設定を採用する。
子プロジェクトで親のナレッジ利用を止めたい場合は、`layers: []`を置く。

`path`は常に設定ファイルの所在ディレクトリから解決する。
初期仕様ではプロジェクト内の相対パスだけを許可し、絶対パス、URL、環境変数展開、`~`展開、globは受け付けない。
記述上の区切りは`/`を使う。
正規化した実体パスがプロジェクト外に出る指定は、`..`やシンボリックリンク経由も含めてエラーにする。
プロジェクトルート自身をレイヤーにする指定と、設定ファイル自体のシンボリックリンクも受け付けない。
外部の共有ナレッジは、当面はプロジェクト内へ取得済みのディレクトリを登録する。

同じ実体パスの二重登録や、レイヤー同士の親子関係もエラーにする。
これにより同じナレッジの二重処理と、更新範囲の重複を避ける。
パスの同一性は実行環境のファイルシステム規則に従って判定する。

## 読み込みとエラーの区別

| 状態 | 動作 |
| --- | --- |
| 設定ファイルなし | 対象外。ナレッジの自動探索や読み書きを行わない |
| 設定あり、全参照先が正常 | 登録されたナレッジを利用する |
| 設定あり、`optional: false`の指定パスが欠落 | 未初期化としてエラー。通常操作では自動作成しない |
| `optional: true`のディレクトリが欠落 | そのレイヤーを省略し、省略したIDを実行結果に示す |
| ディレクトリはあるがmanifestが欠落または不正 | `optional`にかかわらずエラー |
| 設定が不正、読み取り不能、未対応の値を含む | 設定エラー。部分適用や既定値への置換を行わない |

未知キー、重複キー、型不一致をエラーとし、キー名と設定ファイルの位置を報告する。
複数YAML文書、独自タグ、アンカー、エイリアス、マージキーは受け付けない。
`optional`は「ディレクトリがない」という条件だけを緩和し、権限エラーや破損を隠す用途には使わない。
形式チェックは利用に必要な前提確認であり、Knowledgeの正確性を調べる`verify`や修正する`fix`を自動実行するものではない。

## レイヤード運用

将来の複数配置も、同じ`layers`構造で表現する。
次は、3つのナレッジディレクトリを用意した場合の例である。

```yaml
version: "1.0"
layers:
  - id: local
    path: ./.project-knowledge/local
    description: 個人の作業環境や試行結果を記録します。個人環境に関する質問で参照します。
    access: read-write
    optional: true
  - id: team
    path: ./project-knowledge
    description: チームで共有する仕様と設計判断を保存します。プロジェクトの実装や運用を調べるときに参照します。
    access: read-only
  - id: public
    path: ./vendor/public-knowledge
    description: 外部公開された共通知識を参照します。汎用的な概念や公開仕様を調べるときに使用します。
    access: read-only
write_target: local
```

個人用ディレクトリはGitの追跡対象外とし、チーム用と公開用を読みながら個人用に記録できる。
個人用ディレクトリがない環境では読み取りを継続し、書き込み時には個人用の初期化が必要であることを報告する。
`local`、`team`、`public`というIDに予約された動作はなく、利用者が用途を表す名前として付けている。

配列順は探索と表示の順序にだけ使う。
同名ファイルを重ねて仮想的に上書きしたり、先頭レイヤーの主張を自動的に正しいと扱ったりしない。
各Knowledgeは「レイヤーIDとbundle内のパス」で識別し、回答の根拠にも所属レイヤーを保持する。
内容が矛盾した場合は両方の根拠と適用範囲を示す。
将来、明示的な上書きを導入するなら、上書き対象を識別する別仕様が必要になる。

更新操作は1回につき1つの対象レイヤーを選び、そのレイヤーのPolicyを適用する。
`read-only`は本文だけでなく、Policy、ログ、state、キャッシュ、公開成果物を含むレイヤー内への書き込みを禁止する。
利用側の作業状態が必要な場合はプロジェクト側のキャッシュに分離し、レイヤーIDと正規化パスで区別する。
既存の`learning.mode`は更新候補を評価する時機を決め、`access`は保存できるかを決める。
自動更新を許すPolicyでも、読み取り専用レイヤーを更新しない。

検索対象として束ねることと、公開成果物へまとめることは別にする。
公開時には対象レイヤーを明示させ、ローカルやチームの情報を公開用へ自動混入させない。
`public`という名前や読み取り専用指定は、アクセス制御や公開許可の代わりにはならない。

## 既存設定との責務分担

| 保存先 | 管理する内容 |
| --- | --- |
| `project-knowledge.yaml` | プロジェクトから利用するレイヤー、パス、用途の説明、書き込み可否、既定の書き込み先 |
| 各レイヤーの`manifest.yml` | そのナレッジの形式と版 |
| 各レイヤーの`knowledge-policy.md` | 保存価値の判断、`learning.mode`、`knowledge.human_readable` |
| `state.yml`とキャッシュ | 再構築可能な実行状態 |

育成モードと文章方針はルート設定へ移さない。
持ち運ばれたナレッジでも方針を維持でき、ルート設定との優先順位も不要になる。
既存Format 1.0で必須のPolicy設定は引き続き必須であり、ルート設定の省略可能性とは独立して扱う。

追加候補として採用するのは、パスのほかに`id`、`description`、`access`、`optional`、`write_target`である。
用途がまだ確定していない次の項目は、初期仕様から外す。

| 候補 | 見送る理由と将来の検討条件 |
| --- | --- |
| `visibility`、`audience` | ラベルだけでは公開範囲を保証できない。公開時の情報選別と一緒に設計する |
| `priority`、`merge_strategy` | ナレッジの矛盾を設定値の上書きと同様に扱えない。識別と根拠の規則が先に必要 |
| `enabled` | 空の`layers`で利用対象ゼロを表せる。レイヤーの一時切り替えが必要になった段階で検討する |
| `include`、`extends`、ローカル上書きファイル | 再帰参照や配列マージの規則が増える。初期仕様は1ファイルで完結させる |
| リモートURL、認証、同期設定 | 取得と同期は別の操作として設計する。設定の読み込みでは通信しない |
| 検索モデル、埋め込み、件数制限 | 検索実装への依存が強いため、利用レイヤーの宣言から分離する |
| publishの出力形式と出力先 | 現行どおり公開操作の実行時指定とする |
| `schema_version` | ルート設定の版は必須の`version`で管理し、重複する項目は追加しない |

## 段階的な導入と既存プロジェクトの移行

初期実装では設定必須化と1レイヤーの解決を導入し、`layers`は0件または1件だけを受け付ける。
この段階から表のキーを解釈し、2件以上は「複数レイヤー未対応」と明示的にエラーにする。
複数レイヤーの検索、根拠の識別、更新先の選択、キャッシュ分離が揃った時点で件数制限を解除する。
前節の3レイヤー例は、その後の利用例である。

既存プロジェクトは、従来どおりの配置ならルートに初期化サンプルと同じ`project-knowledge.yaml`を追加するだけで登録できる。
`version: "1.0"`、`layers`、`id`、`path`を明記し、従来の更新操作を続ける場合は`access: read-write`を指定する。
既存のナレッジを移動したり、manifestやPolicyを書き換えたりする必要はない。
設定がないプロジェクトへの暗黙の互換動作は用意しない。

明示的な`init`は、対象外のプロジェクトへ設定を作成するための例外とする。
既存ディレクトリがある場合は利用可能性を確認し、既存内容を上書きせず登録する。
新規作成ではナレッジの初期構造を準備し、最後に設定ファイルを作成して登録を完了する。
通常の新規initは`--prepare`で登録前の骨組みを作り、本文生成後にオプションなしで再実行して登録する。空での初期化は1回で登録まで完了する。
登録後は読み取り専用の省略規則を適用する。既存の読み取り専用レイヤーへの再実行は、初期化済みなら変更なしで終了し、修復が必要なら停止する。
設定が既にある場合はそれを尊重し、別の既定パスに作り直さない。

実装時は、各Skillで共通の設定解決処理を使う。
`init_project.py`の固定パス、各Skillの手順と実行例、AGENTS.mdの案内、publishの出力先解決、benchmarkのbaseline判定を更新対象にする。
低水準のbundle検査関数は独立して使えてよいが、ユーザー向けProject Knowledge操作は設定の存在確認を通す。
過去commitを比較するbenchmarkでは、そのcommit自身の設定を使い、設定のないbaselineは未登録と報告する。

## 実装時の受け入れ条件

- 設定がない場合、既存の`project-knowledge/manifest.yml`だけでは有効化しない。
- `version`は文字列`"1.0"`を必須とし、欠落、数値型、未対応値を拒否する。ユーザーの指示なしに版を上げない。
- `layers`の省略、`{}`、空ファイル、コメントだけのファイルを設定エラーにする。
- レイヤー数にかかわらず各要素の`id`と`path`を必須とし、欠落時に値を補わない。
- 子ディレクトリから実行しても、設定所在地を基準に同じパスを得る。
- 明示ルート、Git境界、ワークスペース境界、ネスト設定の探索規則を守る。
- `layers: []`は有効な利用対象ゼロの設定とし、`layers: [{}]`は設定エラーにする。
- `id: default`では`description`の省略を受け付け、それ以外では省略、空文字列、空白だけの文字列を設定エラーにする。
- `description`の複数行文字列を受け付け、説明をLLMの処理判断に利用できるようにする。
- `description`によって書き込み制限、既定の書き込み先、Policyを上書きしない。
- 初期化時のYAMLは必須項目を含み、先頭行に`project-knowledge` Skillで生成したことを示すコメントを持つ。
- 初期化時は`id: default`を使い、`description`、`access`、`optional`、`write_target`を生成しない。
- 未知キー、重複キー、型違い、無効な書き込み先で停止する。
- プロジェクト外への脱出、実体パスの重複、レイヤーの親子関係を拒否する。
- `optional`は欠落だけを許容し、破損や権限不足を隠さない。
- 読み取り専用レイヤーにstateやキャッシュも書かない。
- 書き込み先が使えなくても、別レイヤーへ自動で変更しない。
- 複数対応前は2件以上を拒否し、対応後はレイヤー別の根拠と状態を保持する。
- 既存プロジェクトは設定の追加だけで登録でき、既存の形式と版を維持する。

## 現行仕様の確認元

- [初期化処理](skills/project-knowledge/scripts/init_project.py)：`project_root / "project-knowledge"`による固定パス。
- [設定操作](skills/project-knowledge/references/config.md)：Policy内の運用設定と公開設定の責務。
- [Format 1.0](skills/project-knowledge/references/data-formats/1.0.md)：manifest、Policy、state、bundle内のパス基準。
- [benchmark実行](skills/project-knowledge-benchmark/references/runner.md)：既存ナレッジを含むbaselineの扱い。
