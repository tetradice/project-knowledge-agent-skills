interface:
  display_name: "Project Knowledge Audit"
  short_description: "Knowledge Baseの構造をaudit/refactor"
  default_prompt: "Use $project-knowledge-audit for a read-only audit or an explicitly requested structural refactor of Project Knowledge. Do not refactor unless the user asks for it."
policy:
  allow_implicit_invocation: false
